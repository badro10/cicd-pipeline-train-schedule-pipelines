pipeline {
    agent any
    stages {
        stage('APP Build') {
            steps {
                sh './gradelw build'
                archiveArtifacts artifacts: 'dist/trainSchedule.zip'
            }
        }
    }
}
